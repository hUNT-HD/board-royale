# URL of the Socket.io game server (the Render/Railway deployment).
# Local dev defaults to http://localhost:4000 if unset.
# In Vercel: Project → Settings → Environment Variables → add VITE_SERVER_URL.
VITE_SERVER_URL=https://board-royale-server.onrender.com
