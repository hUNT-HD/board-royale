# Port is provided by the host (Render/Railway) automatically; default 4000 locally.
PORT=4000
# Allowed browser origin for CORS. Set to your Vercel client URL in production.
# Use * only for quick testing.
CLIENT_ORIGIN=https://board-royale.vercel.app
